window.app = window.app || {};

(function($) {

	'use strict';

	/**
	* initialize all
	* @returns {undefined}
	*/
	// var init = function() {
	// 	initBubbleChart();
	// 	initSortingLinks();
	// 	initHighlightLinks();

	// 	// load data and kick things off
	// 	loadData();
	// };
	
	// $(document).ready(init);

})(jQuery);